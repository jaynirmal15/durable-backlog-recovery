FROM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY downstream ./downstream
RUN CGO_ENABLED=0 go build -o /downstream ./downstream

FROM alpine:3.20
COPY --from=build /downstream /downstream
EXPOSE 8080
ENTRYPOINT ["/downstream"]
